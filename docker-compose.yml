services:
  bridge:
    build:
      context: .
      dockerfile: packages/bridge/Dockerfile
    ports:
      - "8333:8333"
      - "9333:9333"
    volumes:
      - bridge-data:/home/relay/.relay-bridge
    restart: unless-stopped

volumes:
  bridge-data:
